plugins {
    // Required for NeoGradle
    id "org.jetbrains.gradle.plugin.idea-ext" version "1.1.7"
    // Required to bundle the toml library
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}